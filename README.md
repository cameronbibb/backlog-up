# Backlog Up

A video game backlog organizer built with Rails and React.

## Structure

- `backend/` - Rails API
- `frontend/` - React + TypeScript (coming soon)

## Setup

### Backend
```
cd backend
bundle install
rails db:create db:migrate
rails server
```
